apiVersion: apps/v1
kind: Deployment
metadata:
  name: blazor-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blazor-backend
  template:
    metadata:
      labels:
        app: blazor-backend
    spec:
      containers:
        - name: blazor-backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 5000
